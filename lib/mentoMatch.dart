import 'package:flutter/cupertino.dart';

class Mento extends StatelessWidget {
  const Mento({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Mento'),
    );
  }
}
